import { DeliveryTimePipePipe } from './delivery-time-pipe.pipe';

describe('DeliveryTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
